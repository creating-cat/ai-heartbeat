# This file defines the cooldown and lock times for specific tools.
# Format: <tool_id>:<cooldown_seconds>:<lock_seconds>
#
# - <tool_id>: The unique identifier for the tool.
# - <cooldown_seconds>: The cooldown period after a 'success' status.
# - <lock_seconds>: The lock period after a 'quota_exceeded' status.

gemini_cli.google_web_search:600:3600
gemini_cli.web_fetch:600:3600